@page "/getIncomeSummary"

@inject BikeSparesInventorySystem.Repositories.Repository<Miners> MinersRepository
@inject BikeSparesInventorySystem.Repositories.Repository<Sales> SalesRepository
@inject BikeSparesInventorySystem.Repositories.Repository<Purchases> PurchaseRepository;
@inject BikeSparesInventorySystem.Repositories.Repository<Expenses> ExpensesRepository;
@inject BikeSparesInventorySystem.Services.AuthService AuthService;

@inject ISnackbar Snackbar

<MudTabs Elevation="0" Outlined="true" @bind-ActivePanelIndex="activeIndex" Color="Color.Secondary">
    <MudTabPanel Text="Daily Income" ID='"pn_one"'>
        <GetIncomeDaily/>
    </MudTabPanel>
    <MudTabPanel Text="Weekly Income" ID='"pn_two"' Disabled>
        <GetIncomeWeekly />
    </MudTabPanel>
    <MudTabPanel Text="Monthly Income" ID='"pn_three"' Disabled>
        <GetIncomeMonthly />
    </MudTabPanel>
</MudTabs>

@code
{
    int activeIndex = 0;

    public const string Route = "/getIncomeSummary";
}