@page "/getIncomeSummary"

@inject BikeSparesInventorySystem.Data.Services.AuthService AuthService;

@inject ISnackbar Snackbar

<MudTabs Elevation="0" Outlined="true" @bind-ActivePanelIndex="activeIndex" Color="Color.Secondary">
    <MudTabPanel Text="Daily Income" ID='"pn_one"'>
        <GetIncomeDaily/>
    </MudTabPanel>
    <MudTabPanel Text="Monthly Income" ID='"pn_three"'>
        <GetIncomeMonthly />
    </MudTabPanel>
</MudTabs>

@code
{
    int activeIndex = 0;

    public const string Route = "/getIncomeSummary";
}